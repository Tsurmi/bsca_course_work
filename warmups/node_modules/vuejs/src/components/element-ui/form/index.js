import Vue from 'vue';
import Main from './main';

Main.install = (Vue) => {
    Vue.component(Main.name, Main);
};

Vue.use(Main);

export default {
    name: 'nftn-el-form-component',
    components: {
        Main
    }
};
