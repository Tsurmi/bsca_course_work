import Vue from 'vue';
import Main from './main';

Main.install = (Vue) => {
    Vue.component(Main.name, Main);
};

Vue.use(Main);

export default {
    name: 'nftn-el-grid-component',
    components: {
        Main
    }
};
